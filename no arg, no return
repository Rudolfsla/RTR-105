#include <stdio.h>
void value(void);
void main()
{
    value();
}
void value(void)
{
    int year = 1, period = 5, amount = 5000;
    float sum;
    sum = amount;
    while (year <= period) {
        sum = sum * (1);
        year = year + 1;
    }
    printf(" The total amount is %f:", sum);
}
